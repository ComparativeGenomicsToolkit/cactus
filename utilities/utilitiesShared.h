#ifndef UTILITIES_SHARED_H_
#define UTILITIES_SHARED_H_

/*
 * Calculates all the bases in a chain, including those in the links.
 */
double calculateChainSize(Chain *chain);

double calculateTotalContainedSequence(Net *net);

#endif
